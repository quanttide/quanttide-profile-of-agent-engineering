# Hermes 配置

| 文件 | 目标路径 | 说明 |
|---|---|---|
| `config.yaml` | `~/.hermes/config.yaml` | 运行时配置（模型、tools、terminal、checkpoints 等） |

> `.env` 含 API 密钥不纳入版本管理，`SOUL.md` 为默认系统提示词（安装可得），均不在此维护。
> 完整配置参考见 `data/library/hermes/config.md`。
