# OpenCode 配置

| 文件 | 目标路径 | 说明 |
|---|---|---|
| `opencode.json` | `~/.config/opencode/opencode.json` | 自定义配置（插件引用），因含机器相关路径不纳入版本管理 |

> 新机器上需手动创建此文件并替换 `plugin` 路径为本机 ponytail 仓库路径。

> 命令文件（ponytail 等）由各自仓库安装，不在此处维护。
> 完整配置参考见 `data/library/opencode/config.md`。
