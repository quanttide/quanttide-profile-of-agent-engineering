# OpenCode 配置

| 文件 | 目标路径 | 说明 |
|---|---|---|
| `opencode.json` | `~/.config/opencode/opencode.json` | 自定义配置（插件引用），`plugin` 路径新机器上需调整 |

> 命令文件（ponytail 等）由各自仓库安装，不在此处维护。
> 完整配置参考见 `data/library/opencode/config.md`。
