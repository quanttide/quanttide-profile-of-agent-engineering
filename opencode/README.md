# OpenCode 配置

| 文件 | 目标路径 | 说明 |
|---|---|---|
| `opencode.json` | `~/.config/opencode/opencode.json` | 自定义配置（插件引用） |

> ponytail: `plugin` 中的路径是机器相关的绝对路径，新机器需改为对应 `ponytail` 仓库的实际路径。

> 命令文件（ponytail 等）由各自仓库安装，不在此处维护。
> 完整配置参考见 `data/library/opencode/config.md`。
